{
  "name": "medikquantis",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.29.1",
  "scripts": {
    "dev": "pnpm --filter @medcalc/web dev",
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint:i18n": "node scripts/check-i18n-parity.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
